# License decision record

**seo-agent is licensed under the MIT License** (SPDX: `MIT`).

- Decided by the owner on 2026-09-25.
- Copyright holder: Fadel Drissi Toubbali (`Copyright (c) 2026 Fadel Drissi Toubbali`).
- Full text: [`LICENSE`](LICENSE). `package.json` declares `"license": "MIT"`.
- `package.json` keeps `"private": true`, so `npm publish` stays refused.
  Publishing a package, creating the public repository, pushing, tagging, or
  changing repository visibility each still need the owner's explicit approval
  (see `docs/RELEASING.md`).

## What the license covers, and what it does not

The MIT License covers **only this project's own source code, prompts,
documentation, and templates**. It does not cover, and cannot grant rights to:

- third-party services and APIs the application connects to (Google Search
  Console, Google Analytics Data, PageSpeed Insights, CrUX, LLM Gateway and the
  model providers behind it, DataForSEO, Apify and the Actor
  `9sHOY9RzPYGjmTHo8`, Reddit content returned by it, Qdrant);
- data you collect through those services (governed by their terms and by law);
- third-party npm packages (each keeps its own license; see
  `THIRD_PARTY_NOTICES.md`, generated by `npm run licenses:check -- --write`);
- container base images, Obsidian (a separate proprietary application that
  seo-agent never bundles), Playwright browsers, or any other external asset;
- trademarks, including the project name.

MIT permits almost any use, including closed-source, commercial, and hosted
reuse, on the condition that the copyright and license notice are kept. That
trade-off (maximum adoption; others may build proprietary or hosted versions
without sharing changes) was accepted when choosing MIT over copyleft options
such as AGPL-3.0.

## Where the license is applied

- `LICENSE` at the repository root (full, unmodified MIT text).
- `package.json` `"license": "MIT"` (and the root entry of `package-lock.json`).
- npm package: `LICENSE` is included automatically by `npm pack`.
- Container image: `.dockerignore` re-includes `LICENSE` and
  `THIRD_PARTY_NOTICES.md`, and the `Dockerfile` copies both into the image.
- `README.md` "License" section and `CHANGELOG.md`.
- Per-file `SPDX-License-Identifier` headers were not added (optional for MIT).

## Contributions

Contributions are accepted under the same MIT License (inbound = outbound).
Whether contributions also require a Developer Certificate of Origin sign-off
or a Contributor License Agreement is **not decided yet**; see
`CONTRIBUTING.md`. Note that relicensing later requires permission from every
copyright holder, which gets harder once outside contributions are merged.

## Verification

`npm run licenses:check` (dependency compatibility and an up-to-date
`THIRD_PARTY_NOTICES.md`) and `npm run release:check` (the `license` item must
pass: `package.json` and `LICENSE` must agree). This is not legal advice.
