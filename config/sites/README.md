# Site configuration examples

Only synthetic examples live here. Real site configuration belongs in your
private workspace (`<workspace>/config/sites/<site-id>.yaml`), which is
created by `npm run cli -- setup`. See docs/CONFIGURATION.md.
