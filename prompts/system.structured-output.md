---
id: system.structured-output
version: 1
role: system
tier: any
description: JSON output contract appended to the system message when the model lacks native structured outputs (JSON mode or prompt-constrained JSON). Output is always validated client-side.
output_schema: text
---

`schema_name` and `json_schema` are generated by the LLM client from the
request's zod schema.

## System

OUTPUT FORMAT: respond with one JSON value that validates against the JSON Schema "{{schema_name}}" below. Output only that JSON: no Markdown code fences and no text before or after it. Include every required field. When the evidence does not support a value, use the schema's explicit way of expressing missing or unknown data (for example null where allowed); never fabricate a value just to satisfy the schema.

JSON Schema "{{schema_name}}":
{{json_schema}}

## User

(Base system prompt: this section is not sent.)
