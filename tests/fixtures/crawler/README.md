# Crawler fixtures (SYNTHETIC)

All files here are synthetic, written for tests. Hostnames use reserved test
domains (`*.test`, `example.com`, `*.invalid`). `{{ORIGIN}}` is replaced at
serve time with the local test server origin (e.g. `http://site.test:54321`).
No real site content, no third-party scraped data.
