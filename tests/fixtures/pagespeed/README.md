# PageSpeed Insights / CrUX fixtures (SYNTHETIC)

Hand-written, reduced responses following the shapes documented in
docs/integration-contracts.md section 5. Values are invented for tests; they
are not measurements of any real site. `www.example.test` is a reserved test
domain. Every JSON file carries `"_synthetic": true`.
